import React, { useEffect } from 'react';

export default function Home() {
    useEffect(() => {
        const tk = localStorage.getItem('token');
    },[]);

    return (
        <div>
         
        </div>
            );
}